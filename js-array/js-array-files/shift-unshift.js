// The unShift method always push element to the start of the array shift method also remove element to the start
const friends = ['farha','arha','tanisha','lamiya'];
// unShift)() method
friends.unshift('saifa');
console.log(friends);

// shift() method
const out1 = friends.shift();
console.log(friends);
console.log(out1);